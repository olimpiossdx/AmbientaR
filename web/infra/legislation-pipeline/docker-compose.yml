services:
  legislation-pipeline:
    build: .
    ports:
      - "8092:8092"
    environment:
      WORKER_SHARED_SECRET: dev-legislation-secret
      ALMG_OPEN_DATA_BASE_URL: https://dadosabertos.almg.gov.br
      # DATABASE_URL: postgresql://ambientar:ambientar@postgres:5432/legislation

  # postgres:
  #   image: pgvector/pgvector:pg16
  #   environment:
  #     POSTGRES_USER: ambientar
  #     POSTGRES_PASSWORD: ambientar
  #     POSTGRES_DB: legislation
  #   ports:
  #     - "5433:5432"
  #   volumes:
  #     - ./schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
