/**
 * Configuração servidor da pasta "termos de referencia" (paths no disco).
 * Importar apenas em API routes / Server Components.
 */

import 'server-only';

export {
  getTermosReferenciaBasePath,
  getTermosReferenciaPathForStudy,
} from '@/lib/termos-referencia-paths';
